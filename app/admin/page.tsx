export default function AdminDashboardPage() {
  return (
    <div>
      <h1 className="font-display text-3xl">Dashboard</h1>
      <p className="mt-2 text-sm text-zinc-600">
        Admin shell. Wire up KPIs (orders today, revenue, low stock) once
        queries are ready.
      </p>
    </div>
  );
}
