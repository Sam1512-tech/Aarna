## What this PR does

<!-- 1–3 bullets. What changed and why. -->

## Type
- [ ] Frontend (UI / components / pages)
- [ ] Backend (schema / actions / integrations / webhooks)
- [ ] Shared (types / utils / config)

## Checklist
- [ ] Branch is up to date with `main` (`git pull --rebase origin main`)
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] No secrets committed (check `.env*` is gitignored)
- [ ] Screenshots attached if UI changed
- [ ] Schema changes generate a new migration in `drizzle/migrations/`

## Notes for reviewer

<!-- Anything tricky to call out. -->
